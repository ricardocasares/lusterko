# Lusterko

A fullscreen Elm webcam party game with a minimal Tailwind CSS score and pose OSD. TypeScript only bridges MediaPipe and configures Vite.

```sh
bun install
bun run dev
```

Webcam access requires localhost or HTTPS. Target poses are rendered directly from the same angle definitions used for matching.
